import { css } from '@emotion/react';

export const optionWrapper = () => css({ display: 'flex', flexDirection: 'column', gap: '16px' });

export const autocomplete = () => ({ width: '100%', maxWidth: 300, height: '100%' });
